import 'node_base.dart';

/// AST Call, direct children of AST Root.
class CallNode extends Node {
  /// TODO: impl. Not sure whether we really need this.
  /// A call node is virtual, it doesn't necessarily to draw as a node.
}
