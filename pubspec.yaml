name: mandrake
description: The Animagus GUI.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  convert: ^2.1.1
  cupertino_icons: ^0.1.3
  protobuf: ^1.0.1
  uuid: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0

flutter:
  uses-material-design: true
